diff --git a/src/frame/headers.rs b/src/frame/headers.rs
index bcab0ed..bd594f9 100644
--- a/src/frame/headers.rs
+++ b/src/frame/headers.rs
@@ -709,14 +709,18 @@ impl Iterator for Iter {
                 return Some(Method(method));
             }
 
-            if let Some(scheme) = pseudo.scheme.take() {
-                return Some(Scheme(scheme));
-            }
-
+            // BOREAS: `:authority` before `:scheme`, the order Chrome emits
+            // and the fourth field of the Akamai HTTP/2 fingerprint. RFC 9113
+            // s8.3 fixes no relative order among pseudo-headers, so this
+            // changes the fingerprint and not the protocol.
             if let Some(authority) = pseudo.authority.take() {
                 return Some(Authority(authority));
             }
 
+            if let Some(scheme) = pseudo.scheme.take() {
+                return Some(Scheme(scheme));
+            }
+
             if let Some(path) = pseudo.path.take() {
                 return Some(Path(path));
             }
